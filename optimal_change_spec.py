import unittest
from optimal_change import optimal_change

class OptimalChange(unittest.TestCase):
	
	def test_1(self):
		self.assertEqual(optimal_change(), )

	def test_2(self):
		self.assertEqual(optimal_change(), )

	def test_3(self):
		self.assertEqual(optimal_change(), )

	def test_4(self):
		self.assertEqual(optimal_change(), )

	def test_5(self):
		self.assertEqual(optimal_change(), )


if __name__ == "__main__":
	unittest.main()